{
  "name": "@dsh-platform/sdk-driver",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "实验性 / 未激活(DEFERRED_WITHOUT_PRODUCTION_PATH):每用户 dsh 运行时驱动骨架,当前无生产调用点,生产 Runtime 走 supervisor spawn",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
